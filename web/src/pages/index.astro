---
// The English landing page (`/`). The template and copy live in the shared
// `Landing` component and `src/i18n/landing.ts`; `/zh/` renders the same
// component with the Chinese table.
import Landing from '../components/Landing.astro';
---

<Landing lang="en" />
